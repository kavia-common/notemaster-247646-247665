# Base URL of the backend API (FastAPI), e.g. http://localhost:3001
NEXT_PUBLIC_API_BASE_URL=

# Optional: if your backend expects a specific audience/issuer for JWT, document here (not required by this frontend by default)
# NEXT_PUBLIC_JWT_ISSUER=
